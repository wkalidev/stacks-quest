;; DEPRECATED — superseded by stacks-quest-v2.clar (the actively deployed contract).
;; Removed from git tracking and from Clarinet.toml during the 2026-07-16 audit.
;; This stub is left on disk only because this environment could not unlink the file;
;; delete it manually (`rm contracts/stacks-quest.clar`) when convenient.
